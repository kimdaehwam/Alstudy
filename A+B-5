#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
	int A, B;
	while (scanf("%d %d", &A, &B) == 2)
	{
		if (A == 0 && B == 0)
			break;
		printf("%d\n", A + B);
	}
	
	return 0;
}
