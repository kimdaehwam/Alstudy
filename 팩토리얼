#include <stdio.h>
#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main()
{
	int N;
	cin >> N;
	int sum = N;
	if (N == 0)
		cout << 1;
	else
	{
		for (int i = N - 1;i >= 1;i--)
			sum = sum * i;
		cout << sum;
	}

	return 0;
}
