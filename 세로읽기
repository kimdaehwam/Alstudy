#include <stdio.h>#include <iostream>using namespace std;char arr[5][16];int main(){ for (int i = 0;i <= 4;i++)  scanf("%s", arr[i]); for (int i = 0;i <= 14;i++) {  for (int j = 0;j <= 4;j++)  {   if (arr[j][i] != 0 || arr[j][i] != '\0')    cout << arr[j][i];  } } return 0;}
