#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
	int N;
	cin >> N;
	while (N--)
	{
		int arr[4];
		scanf("%d %c %d", &arr[1], &arr[2],&arr[3]);
		printf("%d\n", arr[1] + arr[3]);
	}

	return 0;
}
