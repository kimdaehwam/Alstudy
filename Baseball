#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
	int N, A, B;
	cin >> N;
	for (int i = 1;i <= 9;i++)
	{
		scanf("%d %d", &A, &B);
		while (1)
		{
			if (N == 0)
				break;
			if (A > B)
				printf("Yonsei\n");
			else if (A < B)
				printf("Korea\n");
			else if (A == B)
				printf("Draw\n");
			N--;
		}
	}

	return 0;
}
