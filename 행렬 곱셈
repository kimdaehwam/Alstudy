#include <stdio.h>#include <string.h>int main(){ int N, M, K; int left[100][100]; int right[100][100]; int result[100][100]; memset(result, 0, sizeof(result)); scanf("%d %d", &N, &M); for (int i = 0;i <= N - 1;i++) {  for (int j = 0;j <= M - 1;j++)   scanf("%d", &left[i][j]); } scanf("%d %d", &M, &K); for (int i = 0;i <= M - 1;i++) {  for (int j = 0;j <= K - 1;j++)   scanf("%d", &right[i][j]); } for (int i=0;i<=N-1;i++) {   for (int j = 0;j <= K - 1;j++)  {   for (int k = 0;k <= M - 1;k++)   {    result[i][j] = result[i][j] + left[i][k] * right[k][j];   }  } } for (int i = 0;i <= N - 1;i++) {  for (int j=0;j<=K-1;j++)   printf("%d ", result[i][j]);  printf("\n"); }  return 0;}
